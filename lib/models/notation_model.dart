class NotationModel {
  String? userId;
  String? userName;
  String? userImage;
  String? note;
  DateTime? date;

  NotationModel(
      {this.userId, this.userName, this.userImage, this.note, this.date});
}
